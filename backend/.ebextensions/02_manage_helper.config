files:
  "/usr/local/bin/manage":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      set -a
      source /opt/elasticbeanstalk/deployment/env
      set +a
      cd /var/app/current
      exec $(ls -d /var/app/venv/*/bin/python | head -n 1) manage.py "$@"
