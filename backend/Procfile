web: python manage.py migrate --noinput && gunicorn core.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers ${WEB_WORKERS:-2} --timeout 120
worker: celery -A core worker --loglevel=info --concurrency=2 --max-tasks-per-child=100
